const PendengaranPresenter = {
    load(data, setData) {
      if (data) {
        setData(data);
      } else {
        setData(null);
      }
    },
  };
  
  export default PendengaranPresenter;
  